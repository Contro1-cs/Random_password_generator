#include<bits/stdc++.h>
using namespace std;

int main()
{
    int lower = 0, upper = 9,length=64,count=1000;
    int rand1;
    srand(time(0));
    for(int j=0;j<count;j++){
        for(int i=0;i<length;i++){
            int num = (rand() % (upper - lower + 1)) + lower;
            if(num%2==0){
                rand1 = (rand() % (57 - 48 + 1)) + 48;
            }else{
                if(num<5){
                rand1 = (rand() % (122 - 97 + 1)) + 97;
                }else{
                    rand1 = (rand() % (90 - 65 + 1)) + 65;
                }
        }
            cout<<char(rand1);
        }
        cout<<endl;
    }
}
